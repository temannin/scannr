{
  "name": "scannr",
  "version": "1.0.0",
  "description": "Utility for auto-scanning documents to the cloud",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"npm run dev --prefix ./api/\" \"npm start --prefix ./ui/\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^5.3.0"
  }
}
